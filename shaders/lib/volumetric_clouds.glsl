/* MakeUp Ultra Fast - volumetric_clouds.glsl
Fast volumetric clouds - MakeUp implementation
*/

vec2 get_cloud(vec3 view_vector) {
  vec2 cloud = vec2(0.0);

  if (camera_position.y < MIN_CLOUD_PLANE) {
    
  }

  return cloud;
}
